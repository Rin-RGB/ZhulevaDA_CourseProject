
# Спецификация API (REST)

**Базовый URL:** `http://localhost:3000/api`

**Формат данных:** JSON

**Аутентификация:** JWT токен передаётся в заголовке `Authorization: Bearer <token>`

---

## 1. Аутентификация (`/auth`)

### 1.1 Регистрация пользователя руководителем

`POST /auth/register`

Руководитель регистрирует работника с помощью формы, id задаётся автоматически с помощью nanoid, password = null, authorized = false

**Тело запроса:**
```json
{
  "email": "worker@bakery.com",
  "name": "Иван",
  "last_name": "Петров",
  "role": "worker",
  "factory_id": 2,
  "role_at_factories": ["worker", "manager"]
}
```

**Ответ:** `201 Created`
```json
{
  "id": 5,
  "message": "Пользовательская почта зарегистрирована"
}
```

**Права доступа:** руководитель завода (только в "свои" заводы), либо руководитель комбината

---
### 1.2 Регистрация пользователя

`POST /auth/register-user`

Пользователь устанавливает пароль для почты, которая уже есть в базе данных

**Тело запроса:**
```json
{
  "email": "worker@bakery.com",
  "password": "pass123"
}
```

**Ответ:** `201 Created`
```json
{
  "id": 5,
  "message": "Пользователь зарегистрирован"
}
```

---


### 1.3 Вход в систему

`POST /auth/login`
При входе создаётся refresh токен, который добавляется в cookie

**Тело запроса:**
```json
{
  "email": "user@example.com",
  "password": "pass123"
}
```

**Ответ:** `200 OK`
```json
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
}
```

---

### 1.4 Обновление токена

`POST /auth/refresh`

**Cookie:** `refresh_token: "eyJhbGciOiJIUzI1NiIs..."`

**Ответ:** `200 OK`
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs..."
}
```

---

### 1.5 Выход из системы

`POST /auth/logout`

**Заголовки:** `Authorization: Bearer <token>`

**Ответ:** `200 OK`
```json
{
  "message": "Выход выполнен"
}
```

### 1.6 Регистрация пользователем

`POST /auth/activate`

**Тело запроса**

```json
{
  "email": "worker@bakery.com",
  "password": "new_password123"
}
```
**Ответ:** `200 OK`
```json
{   "message": "Аккаунт активирован"    }
```

---

## 2. Текущий пользователь (`/me`)

### 2.1 Информация о текущем пользователе

`GET /me`

**Заголовки:** `Authorization: Bearer <token>`

**Ответ:** `200 OK`
```json
{
    "id": 2,
    "email": "worker@bakery.com",
    "name": "Иван",
    "last_name": "Петров",
    "factories" : [
        { "factory_id": 1, "name": "Завод №1", "role": "manager" },
        { "factory_id": 2, "name": "Завод №2", "role": "worker" }
    ],
    "role": "manager"
}
```
---

## 3. Изделия (`/products`)

### 3.1 Список всех изделий

`GET /products/?factory={factory_id}&sort={string}&limit={10}&offset={0}&search={string}`

**Параметры запроса:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `factory` | integer | ID завода, позволяет задать фильтрацию по заводу |
| `sort` | string | 'ingredients' или 'profit' - сортировка по количеству ингредиентов или по прибыли соответственно |
| `limit` | integer | Количество товаров на странице |
| `offset` | integer | Отступ от начала массива для переключения страниц |
| `search` | string | Поиск по названию |

**Пример:** `GET /products?factory=1&limit=10&offset=0``

**Ответ:** `200 OK`
```json
{"products": [
  {
    "id": 1,
    "name": "Батон нарезной",
    "weight": 0.5,
    "expiration_days": 3,
    "price": 120.00,
    "profit": 50.60,
    "ingredients_count": 4,
  }
],
"summary": 55000}
```

**Права доступа:** все авторизованные

---

### 3.2 Информация об изделии

`GET /products/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID изделия |

**Ответ:** `200 OK`
```json
{
    "id": 1,
    "name": "Батон нарезной",
    "weight": 0.5,
    "expiration_days": 3,
    "price": 120.00,
    "profit": 50.60,
    "ingredients_count": 4,
    "factories": [
        { "factory_id": 1, "name": "Завод №1", "address": "ул. Хлебная, 1",  "total_produced": 20 },
        { "factory_id": 2, "name": "Завод №2", "address": "ул. Пирогова, 1",  "total_produced": 50 }
    ]
}
```
**Права доступа:** все авторизованные

---
### 3.3 Добавить новое изделие

`POST /products`

**Тело запроса:**
```json
{
    "name": "Батон нарезной",
    "weight": 0.5,
    "expiration_days": 3,
    "price": 120.00,
    "ingredients": [
      { "ingredient_id": 1, "quantity_kg": 0.020 },
      { "ingredient_id": 2, "quantity_kg": 0.480 }
    ],
    "factories": [
      { "factory_id": 1 },
      { "factory_id": 2 }
    ]
}
```

**Ответ:** `201 OK`
```json
{
    "id": 1,
    "name": "Батон нарезной",
    "weight": 0.5,
    "expiration_days": 3,
    "price": 120.00,
    "profit": 50.60,
    "ingredients_count": 2,
    "factories": [
        { "factory_id": 1, "name": "Завод №1", "address": "ул. Хлебная, 1" },
        { "factory_id": 2, "name": "Завод №2", "address": "ул. Пирогова, 1" }
    ]
}
```
**Права доступа:** Руководитель завода (руководитель комбината может создать ингредиент с factory: none для хранения информации до распределения на заводы)

---
### 3.4 Изменить информацию об изделии

`PUT /products/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID изделия |

**Тело запроса:**
```json
{
    "name": "Батон нарезной",
    "weight": 0.5,
    "expiration_days": 3,
    "price": 120.00,
    "ingredients": [
        { "ingredient_id": 1, "quantity_kg": 0.020 },
        { "ingredient_id": 2, "quantity_kg": 0.480 }
    ],
}
```

**Ответ:** `201 OK`
```json
{
    "id": 1,
    "name": "Батон нарезной",
    "weight": 0.5,
    "expiration_days": 3,
    "price": 120.00,
    "profit": 50.60,
    "ingredients_count": 4
}
```
**Права доступа:** Руководитель комбината

---
### 3.5 Изменить состав заводов

`PUT /products/{id}/factories`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID изделия |

**Тело запроса:**
```json
{
    "factories": [{ "factory_id": 1 }, { "factory_id": 2 }]
}
```

**Ответ:** `201 OK`
```json
{
    "id": 1,
    "name": "Батон нарезной",
    "factories": [
        { "factory_id": 1, "name": "Завод №1", "address": "ул. Хлебная, 1", "total_produced": 20 },
        { "factory_id": 2, "name": "Завод №2", "address": "ул. Пекарная, 5", "total_produced": 0 }
    ]
}
```
**Права доступа:** Руководитель комбината

---
### 3.6 Удаление изделия

`DELETE /products/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID изделия |

**Ответ:** `204 OK`

**Права доступа:** руководитель комбината

---

### 3.7 Ингредиенты изделия

`GET /products/{id}/ingredients`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID изделия |

**Ответ:** `200 OK`
```json
[
  {
    "ingredient_id": 1,
    "name": "Мука",
    "quantity_kg": 0.8
  },
  {
    "ingredient_id": 2,
    "name": "Дрожжи",
    "quantity_kg": 0.1
  }
]
```
**Права доступа:** все авторизованные

---

## 4. Заводы (`/factories`)

### 4.1 Список всех заводов

`GET /factories?sort={string}`

**Параметры запроса:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `sort` | string | 'total_value' или 'count' - сортировка по суммарной стоимости или по объёму производства соответственно |

**Пример:** `GET /factories?sort="count"`


**Ответ:** `200 OK`
```json
[
    {
        "id": 1,
        "name": "Завод №1",
        "address": "ул. Хлебная, 1",
        "total_value": 8000.00,
        "volume": 15 
    },
    { 
        "id": 2, 
        "name": "Завод №2", 
        "address": "ул. Пекарная, 5", 
        "total_value": 5000.00, 
        "volume": 8  
    }
]
```
**Права доступа:** Руководитель завода или комбината

---

### 4.2 Информация о заводе

`GET /factories/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID завода |

**Ответ:** `200 OK`
```json
{
  "id": 1,
  "name": "Завод №1",
  "address": "ул. Хлебная, 1",
  "total_value": 8000.00,
  "volume": 15,
  "managers":
  [
    {
      "manager_id": 1,
      "name": "Анна",
      "last_name": "Королёва",
      "role": "CEO"
    }
  ]
}
```
**Права доступа:** Руководитель завода или комбината

---
### 4.3 Создание завода

`POST /factories`

**Тело запроса**

```json
{
  "name": "Завод №1",
  "address": "ул. Хлебная, 1"
}
```

**Ответ:** `201 OK`
```json
{
  "id": 1,
  "name": "Завод №1",
  "address": "ул. Хлебная, 1"
}
```
**Права доступа:** Руководитель комбината

---
### 4.4 Изменение завода

`PUT /factories/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID завода |

**Тело запроса**

```json
{
  "name": "Завод №1",
  "address": "ул. Хлебная, 1"
}
```

**Ответ:** `201 OK`
```json
{
  "id": 1,
  "name": "Завод №1",
  "address": "ул. Хлебная, 1"
}
```
**Права доступа:** Руководитель этого завода или комбината

---

### 4.5 Удаление завода

`DELETE /factories/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID завода |


**Ответ:** `204 OK`

**Права доступа:** Руководитель комбината

---

### 4.6 Добавить изделие на завод

`POST /factories/{id}/products`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID завода |

**Тело запроса:**
```json
{
  "product_id": 5
}
```

**Ответ:** `201 Created`
```json
{
  "message": "Изделие добавлено"
}
```

**Права доступа:** руководитель завода (своего), руководитель комбината

---

### 4.7 Удалить изделие с завода

`DELETE /factories/{id}/products/{product_id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID завода |
| `product_id` | integer | ID изделия |

**Ответ:** `200 OK`
```json
{
  "message": "Изделие удалено с завода"
}
```

**Права доступа:** руководитель завода (своего), руководитель комбината

---


## 5. Сотрудники (`/workers`)

### 5.1 Список сотрудников

`GET /workers?factory={id}&role={string}&limit={10}&offset={0}&search={string}`

**Параметры запроса:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `factory` | integer | Фильтр по заводу |
| `role` | string | Фильтр по роли (`worker`, `manager`, `CEO`) |
| `limit` | integer | Количество записей |
| `offset` | integer | Отступ для пагинации |
| `search` | string | Поиск по имени и фамилии |

**Пример:** `GET /workers?factory=1&role=worker`

**Ответ:** `200 OK`
```json
[
  {
    "id": 2,
    "email": "worker@bakery.com",
    "name": "Иван",
    "last_name": "Петров",
    "role": "worker",
    "authorized": true,
    "factories": [
        { "factory_id": 1, "name": "Хлебозавод №1", "role": "worker" }
    ]
  }
]
```

**Права доступа:** Руководитель завода или комбината

---

### 5.2 Информация о сотруднике

`GET /workers/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID сотрудника |

**Ответ:** `200 OK`
```json
{
  "id": 2,
  "email": "worker@bakery.com",
  "name": "Иван",
  "last_name": "Петров",
  "role": "worker",
  "authorized": true,
  "factories": [
    { "factory_id": 1, "name": "Хлебозавод №1", "role": "worker" },
    { "factory_id": 2, "name": "Хлебозавод №2", "role": "worker" }
  ]
}
```

Руководитель завода или комбината

---

### 5.3 Создание сотрудника (регистрация почты)

`POST /workers`

**Тело запроса:**
```json
{
  "email": "new_worker@bakery.com",
  "name": "Пётр",
  "last_name": "Сидоров",
  "factories": [
    { "factory_id": 1, "role": "worker" }
  ]
}
```

**Ответ:** `201 Created`
```json
{
  "id": 6,
  "message": "Сотрудник добавлен"
}
```

**Права доступа:** директор комбината (может создать любого), руководитель завода (может создать только `worker` для своего завода)


### 5.4 Обновление информации о сотруднике

`PUT /workers/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID сотрудника |

**Тело запроса:**
```json
{
  "name": "Пётр",
  "last_name": "Иванов",
  "factories": [
    { "factory_id": 1, "role": "manager" },
    { "factory_id": 2, "role": "worker" }
  ]
}
```

**Ответ:** `200 OK`
```json
{
  "id": 6,
  "message": "Данные сотрудника обновлены"
}
```

**Права доступа:** Руководитель завода или комбината

---

### 5.5 Удаление сотрудника

`DELETE /workers/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID сотрудника |

**Ответ:** `204 OK`

**Права доступа:** Руководитель завода или комбината

---

## 6. Ингредиенты (`/ingredients`)

### 6.1 Список ингредиентов

`GET /ingredients`

**Ответ:** `200 OK`
```json
[
  {
    "id": 1,
    "name": "Мука",
    "price": 35.00,
    "expiration_days": 365
  }
]
```

---

### 6.2 Информация об ингредиенте

`GET /ingredients/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID ингредиента |

**Ответ:** `200 OK`
```json
{
  "id": 1,
  "name": "Мука",
  "price": 35.00,
  "expiration_days": 365

}
```

**Права доступа:** все авторизованные

---

### 6.3 Поставки ингредиентов

`GET /ingredients/batches?ingredient_id={integer}&factory_id={integer}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `ingredient_id` | integer | ID ингредиента |
| `factory_id` | integer | ID завода |
| `limit` | integer | Количество записей (по умолч. 10) |
| `offset` | integer | Смещение (по умолч. 0) |

**Ответ:** `200 OK`
```json
[
  {
    "id": 1,
    "ingredient_id": 12,
    "ingredient_name": "Дрожжи",
    "factory_id": 3,
    "factory_name": "Завод №3",
    "delivery_kg": 100,
    "delivery_date": "2024-05-01",
    "expiry_date": "2024-05-15",
    "is_fresh": true
  }
]
```

**Права доступа:** все авторизованные


### 6.4 Информация о конкретной поставке

`GET /ingredients/batches/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID партии поставки |

**Ответ:** `200 OK`
```json
{
  "id": 1,
  "ingredient_id": 12,
  "ingredient_name": "Дрожжи",
  "factory_id": 3,
  "factory_name": "Завод №3",
  "delivery_kg": 100,
  "delivery_date": "2024-05-01",
  "expiry_date": "2024-05-15",
  "is_expired": false
}
```

**Права доступа:** все авторизованные

---

### 6.5 Регистрация новой поставки ингредиентов

`POST /ingredients/batches`

**Тело запроса:**
```json
{
  "factory_id": 1,
  "ingredient_id": 2,
  "delivery_kg": 100,
}
```

**Ответ:** `201 Created`
```json
{
  "id": 1,
  "ingredient_id": 12,
  "ingredient_name": "Дрожжи",
  "factory_id": 3,
  "factory_name": "Завод №3",
  "delivery_kg": 100,
  "delivery_date": "2024-05-01",
  "expiry_date": "2024-05-15",
}
```

**Права доступа:** рабочий (свой завод) руководитель завода или комбината

---

### 6.6 Удалить поставку

`DELETE /ingredients/batches/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `batch_id` | integer | ID партии поставки |

**Ответ:** `204 OK`

**Права доступа:** все авторизованные

---

### 6.7 Создать новый ингредиент (справочник)

`POST /ingredients`

**Тело запроса:**
```json
{
  "name": "Мука пшеничная",
  "price": 35.00,
  "expiration_days": 365

}
```

**Ответ:** `201 Created`
```json
{
  "id": 10,
  "message": "Ингредиент создан"
}
```

**Права доступа:** руководитель комбината

---

### 6.8 Изменить ингредиент (справочник)

`PUT /ingredients/{ingredient_id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `ingredient_id` | integer | ID ингредиента |

**Тело запроса:**
```json
{
  "name": "Мука пшеничная",
  "price": 35.00,
  "expiration_days": 365

}
```

**Ответ:** `201 OK`
```json
{
  "id": 10,
  "name": "Мука пшеничная",
  "price": 35.00,
  "expiration_days": 365
}
```

**Права доступа:** руководитель комбината

---

### 6.9 Удалить ингредиент (справочник)

`DELETE /ingredients/{ingredient_id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `ingredient_id` | integer | ID ингредиента |


**Ответ:** `204 OK`

**Права доступа:** руководитель комбината

---

## 7. Партии выпуска изделий (`/batches`)

### 7.1 Список всех партий

`GET /batches?factory={integer}&limit={integer}&offset={integer}`

**Параметры запроса:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `factory` | integer | ID завода |
| `product` | integer | ID изделия |
| `limit` | integer | Количество записей (по умолч. 10) |
| `offset` | integer | Смещение (по умолч. 0) |

**Пример:** `GET /batches?limit=5`

**Ответ:** `200 OK`
```json
[
  {
    "id": 10,
    "product_id": 1,
    "product_name": "Батон нарезной",
    "factory_id": 1,
    "factory_name": "Хлебозавод №2", 
    "amount": 500,
    "production_date": "2024-05-20",
    "expiration_date": "2024-05-30",
    "is_fresh": false
  }
]
```

**Права доступа:** работник и руководитель завода (только свои заводы), руководитель комбината

---

### 7.2 Информация о партии

`GET /batches/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID партии |

**Ответ:** `200 OK`
```json
{
    "id": 10,
    "product_id": 1,
    "product_name": "Батон нарезной",
    "factory_id": 1,
    "factory_name": "Хлебозавод №2", 
    "amount": 500,
    "production_date": "2024-05-20",
    "expiration_date": "2024-05-30",
    "is_fresh": false
}
```

**Права доступа:** работник и руководитель завода (только свои заводы), руководитель комбината


### 7.3 Добавить партию

`POST /batches`

**Тело запроса:** 
```json
{
    "product_id": 1,
    "factory_id": 1, 
    "amount": 500,
}
```

**Ответ:** `200 OK`
```json
{
    "id": 10,
    "product_id": 1,
    "product_name": "Батон нарезной",
    "factory_id": 1, 
    "factory_name": "Хлебозавод №2",
    "amount": 500,
    "production_date": "2024-05-20",
    "expiration_date": "2024-05-30",
    "is_fresh": false
}
```
**Права доступа:**  все авторизованные

---

### 7.4 Удаление партии

`DELETE /batches/{id}`

**Параметры пути:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `id` | integer | ID партии |

**Ответ:** `204 OK`

**Права доступа:** работник и руководитель завода (только свои заводы), руководитель комбината
## Коды ответов HTTP

| Код | Описание |
|-----|----------|
| 200 | Успешный запрос |
| 201 | Успешное создание |
| 204 | Успешное удаление |
| 400 | Ошибка валидации |
| 401 | Не авторизован |
| 403 | Недостаточно прав |
| 404 | Ресурс не найден |
| 500 | Внутренняя ошибка сервера |

---